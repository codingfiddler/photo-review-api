version: '3'
services:
  postgres:
    image: postgres
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres

  django:
    build: .
    volumes:
      - .:/app
    ports:
      - "8080:8080"
    depends_on:
      - postgres
